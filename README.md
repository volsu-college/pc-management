# Скрипт управления ПК

Интерактивный bash-скрипт для управления конфигурацией прокси и установки программного обеспечения на системах Linux (Fedora/RHEL).

## Возможности

### 1. Управление прокси
- **Установка прокси** - настройка прокси для:
  - DNF (HTTP/HTTPS)
  - Bash переменные окружения (HTTP/HTTPS/FTP)
  - rsync
  - FTP клиенты
- **Отключение прокси** - удаление всех параметров прокси из системы
- **Просмотр статуса** - отображение текущих настроек прокси

### 2. Установка программного обеспечения
- **Visual Studio Code** - установка редактора кода
- **PyCharm Community** - установка IDE для Python
- **libvirt (QEMU-KVM)** - установка системы виртуализации с поддержкой графического менеджера virt-manager
  - Создаёт директорию `/home/$USER/virtmachine/images` для хранения образов ВМ
  - Устанавливает права доступа rwx для текущего пользователя
- **Установить всё** - установка всех приложений сразу

### 3. Управление пользователями
- **Создать/обновить пользователя 'student'** - создание учётной записи с ограниченными правами для работы с виртуальными машинами

### 4. Копирование
- **Виртуальные машины** - резервное копирование и восстановление виртуальных машин
  - Создание резервных копий в директории `./vms/backup_YYYYMMDD_HHMMSS`
  - Сохранение конфигураций и дисков ВМ
  - Восстановление ВМ на других ПК из резервных копий

## Быстрый запуск

### Способ 1: Загрузить и выполнить напрямую из интернета

```bash
bash <(curl -fsSL https://static.nn-projects.ru/setup.sh)
```

### Способ 2: Загрузить, сделать исполняемым и запустить

```bash
curl -fsSL https://static.nn-projects.ru/setup.sh -o setup.sh
chmod +x setup.sh
./setup.sh
```

### Способ 3: Использовать wget

```bash
wget -qO- https://static.nn-projects.ru/setup.sh | bash
```

## Использование

После запуска скрипта вы увидите главное меню:

```
╔════════════════════════════════════════╗
║     Управление ПК                      ║
╚════════════════════════════════════════╝
1. Управление прокси
2. Установка программного обеспечения
3. Выход
Выберите опцию:
```

### Управление прокси

1. Выберите опцию **1** из главного меню
2. Выберите действие:
   - **1** - Установить прокси (введите только IP адрес, например: `10.10.9.1`)
     - Скрипт автоматически создаст URL: `http://10.10.9.1:3127`
   - **2** - Отключить прокси
   - **3** - Показать текущий статус прокси
   - **4** - Вернуться в главное меню

### Установка ПО

1. Выберите опцию **2** из главного меню
2. Выберите приложение для установки:
   - **1** - Visual Studio Code
   - **2** - PyCharm Community
   - **3** - Управление libvirt (QEMU-KVM) - подменю с опциями установки и управления
   - **4** - Установить всё
   - **5** - Вернуться в главное меню

#### Подменю libvirt (QEMU-KVM)

1. Выберите опцию **3** из меню программного обеспечения
2. Выберите действие:
   - **1** - Установить libvirt с необходимыми пакетами
   - **2** - Проверить и исправить права доступа на директорию виртуальных машин
   - **3** - Вернуться в меню программного обеспечения

## Примеры команд

### Установка прокси для корпоративной сети

```bash
# Запустить скрипт
bash <(curl -fsSL https://static.nn-projects.ru/setup.sh)

# Выбрать опцию 1 (Управление прокси)
# Выбрать опцию 1 (Установить прокси)
# Ввести IP адрес прокси: 10.10.9.1
# Скрипт автоматически создаст URL: http://10.10.9.1:3127
```

### Установка всего ПО через интерактивное меню

```bash
bash <(curl -fsSL https://static.nn-projects.ru/setup.sh)

# Выбрать опцию 2 (Установка ПО)
# Выбрать опцию 3 (Установить всё)
```

## Требования

- Linux система (Fedora, RHEL, CentOS)
- Установлены утилиты: `curl` или `wget`
- Доступ с правами `sudo` для установки ПО и конфигурирования системы
- Bash оболочка
- Для libvirt: поддержка виртуализации процессором (флаги VMX/SVM)

## Изменения в системе

### При установке прокси скрипт:
1. Модифицирует файл `/etc/dnf/dnf.conf` (требует sudo)
2. Добавляет переменные окружения в `~/.bashrc`:
   - `HTTP_PROXY`, `http_proxy`
   - `HTTPS_PROXY`, `https_proxy`
   - `FTP_PROXY`, `ftp_proxy`
3. Создаёт конфигурацию rsync в `~/.rsync/rsync.conf`

### При отключении прокси:
1. Удаляет параметры прокси из `/etc/dnf/dnf.conf`
2. Удаляет переменные окружения из `~/.bashrc`
3. Удаляет конфигурацию rsync из `~/.rsync/rsync.conf`

### При установке ПО:
1. Использует `dnf install` для установки приложений
2. Требует прав администратора
3. Для libvirt:
   - Устанавливает пакеты: libvirt, qemu-kvm, virt-install, virt-manager, openssh-askpass, OVMF и зависимости
   - Запускает и добавляет в автозагрузку службу libvirtd
   - Добавляет текущего пользователя в группу libvirt (требуется перезагрузка)
   - Создаёт виртуальный сетевой интерфейс virbr0

## Вывод скрипта

Скрипт использует цветной вывод для лучшей читаемости:

- 🔵 **[ИНФО]** - информационные сообщения (синий)
- 🟢 **[УСПЕХ]** - успешные операции (зелёный)
- 🟡 **[ПРЕДУПРЕЖДЕНИЕ]** - предупреждения (жёлтый)
- 🔴 **[ОШИБКА]** - ошибки (красный)

## Обработка ошибок

Скрипт предусматривает обработку ошибок:
- Проверка наличия файлов перед редактированием
- Проверка установки приложений перед началом установки
- Обработка неверного ввода пользователя

## Поддержка

Для вопросов и проблем обратитесь к администратору системы.

---

**Последнее обновление:** 27 октября 2025 г.
